# Дублирующие станки и overlap — не в текущем MVP

Документ **архивный**: сценарий с двумя фрезами и двумя токарными, overlap черновая/чистовая и закрытие смены описан для **расширенной** версии, которая в коде (май 2026) **удалена**.

## Текущее состояние

- Демо-парк: **6 станков**, один станок на capability (`DemoFactoryCatalog`).
- Планирование **24/7**, без `workWindows` и `scheduling.overlapBatches`.
- Файлы `data/schedule-duplicate-machines.example.json` и `scripts/prep-duplicate-machines-demo.sh` **не соответствуют** планировщику — не используйте для согласования MVP.

## Что проверять сейчас

Ручные сценарии: [согласование-с-директором.md](согласование-с-директором.md).

Автотесты пакетного планирования на одном станке: `DemoScheduleTest`, `GreedySchedulerTest`, `ScheduleMetricsTest`.

## Планируемая следующая фаза (ориентир)

1. Несколько станков с одной capability в snapshot.
2. Распределение штук между станками одной операции.
3. Опциональный overlap пакетов (`overlapBatches`).
4. Смены и закрытие смены с перепланом.

При возврате функций — обновить этот документ и восстановить example/скрипты.
