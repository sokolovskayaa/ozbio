(function(){const addOrderForm=document.getElementById('add-order-form');if(!addOrderForm)return;const msgEl=document.getElementById('controls-message');let partCatalog=[];try{partCatalog=JSON.parse(document.getElementById('part-catalog-json').textContent||'[]');}catch(e){}function showMsg(text,isError){if(!msgEl)return;msgEl.textContent=text;msgEl.className='controls-message '+(isError?'error':'ok');msgEl.classList.remove('hidden');}function hideMsg(){if(msgEl)msgEl.classList.add('hidden');}function buildPartSelect(selectedId){const sel=document.createElement('select');sel.required=true;sel.className='order-part-select';const empty=document.createElement('option');empty.value='';empty.textContent='— выберите деталь —';sel.appendChild(empty);partCatalog.forEach(function(p){const opt=document.createElement('option');opt.value=p.partId;opt.textContent=p.title+(p.priority!=null?' (приор. '+p.priority+')':'');if(p.partId===selectedId)opt.selected=true;sel.appendChild(opt);});return sel;}function addOrderLine(partId,qty){const box=document.getElementById('order-lines');if(!box)return;const row=document.createElement('div');row.className='order-line';const selWrap=document.createElement('label');selWrap.className='order-line-part';selWrap.textContent='Деталь ';selWrap.appendChild(buildPartSelect(partId));const qtyWrap=document.createElement('label');qtyWrap.className='order-line-qty';qtyWrap.textContent='Кол-во ';const qtyIn=document.createElement('input');qtyIn.type='number';qtyIn.min='1';qtyIn.max='9999';qtyIn.value=String(qty!=null?qty:1);qtyIn.required=true;qtyIn.className='order-qty-input';qtyWrap.appendChild(qtyIn);const rm=document.createElement('button');rm.type='button';rm.className='btn-icon';rm.title='Удалить строку';rm.textContent='×';rm.addEventListener('click',function(){if(box.querySelectorAll('.order-line').length>1)row.remove();});row.appendChild(selWrap);row.appendChild(qtyWrap);row.appendChild(rm);box.appendChild(row);}const addLineBtn=document.getElementById('order-add-line');if(addLineBtn){addLineBtn.addEventListener('click',function(){addOrderLine();});const lines=document.getElementById('order-lines');if(lines&&!lines.children.length){const defaultPart=partCatalog.length?partCatalog[0].partId:null;addOrderLine(defaultPart,1);}}addOrderForm.addEventListener('submit',function(ev){ev.preventDefault();hideMsg();const parts=[];const seen=new Set();let duplicatePart=false;document.querySelectorAll('#order-lines .order-line').forEach(function(row){const pid=row.querySelector('.order-part-select')?.value;const q=parseInt(row.querySelector('.order-qty-input')?.value||'1',10);if(!pid)return;if(seen.has(pid)){showMsg('Деталь «'+pid+'» указана дважды — объедините в одну строку',true);duplicatePart=true;return;}seen.add(pid);parts.push({partId:pid,quantity:q});});if(duplicatePart)return;if(!parts.length){showMsg('Добавьте хотя бы одну деталь',true);return;}const submitBtn=document.getElementById('order-submit');if(submitBtn)submitBtn.disabled=true;fetch('/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({parts:parts})}).then(function(r){return r.json().then(function(j){return {ok:r.ok,status:r.status,body:j};});}).then(function(res){if(submitBtn)submitBtn.disabled=false;if(!res.ok){showMsg(res.body&&res.body.error?res.body.error:('Ошибка '+res.status),true);return;}const id=res.body&&res.body.orderId?res.body.orderId:'';showMsg('Заказ '+(id||'')+' добавлен',false);setTimeout(function(){location.reload();},600);}).catch(function(err){if(submitBtn)submitBtn.disabled=false;showMsg('Сеть: '+err,true);});});document.querySelectorAll('.btn-add-order-jump').forEach(function(btn){btn.addEventListener('click',function(){setTimeout(function(){const sel=document.querySelector('#order-lines .order-part-select');if(sel)sel.focus();},300);});});})();
