(function(){const ordersBox=document.getElementById('filter-orders');const partsBox=document.getElementById('filter-parts');const resetBtn=document.getElementById('filter-reset');const panel=document.getElementById('machine-schedule-panel');const emptyMsg=document.getElementById('machine-filter-empty');const legendEl=document.getElementById('color-legend');let orderParts={};let partTitles={};let scheduleMeta={orders:[],partPriority:{}};try{orderParts=JSON.parse(document.getElementById('order-parts-json').textContent||'{}');}catch(e){}try{partTitles=JSON.parse(document.getElementById('part-titles-json').textContent||'{}');}catch(e){}try{scheduleMeta=JSON.parse(document.getElementById('schedule-meta-json').textContent||'{}');}catch(e){}const allOrderIds=(scheduleMeta.orders||[]).map(function(o){return o.id;});const zoomPresets=scheduleMeta.zoomPresets||{};const zoomLabelEl=document.getElementById('zoom-range-label');const zoomPrevBtn=document.getElementById('zoom-prev');const zoomNextBtn=document.getElementById('zoom-next');const zoomCustomStart=document.getElementById('zoom-custom-start');const zoomCustomEnd=document.getElementById('zoom-custom-end');const zoomCustomApply=document.getElementById('zoom-custom-apply');const TZ=scheduleMeta.timeZone||'Europe/Moscow';function partLabel(id){return partTitles[id]||id;}function partPriority(id){const p=scheduleMeta.partPriority||{};return p[id]!=null?Number(p[id]):Number((panel.querySelector('[data-part=\"'+id+'\"]')||{}).dataset?.partPriority||0);}function orderCreatedMs(id){const o=(scheduleMeta.orders||[]).find(function(x){return x.id===id;});return o?Number(o.createdAtMs):0;}function selectedValues(box){return Array.from(box.querySelectorAll('input[type=checkbox]:checked')).map(function(i){return i.value;});}function partKey(oid,pid){return oid+'::'+pid;}function rebuildPartFilters(){const prev=new Set(selectedValues(partsBox));const selectedOrders=selectedValues(ordersBox);const orderScope=selectedOrders.length?selectedOrders:allOrderIds;const showOrderInLabel=allOrderIds.length>1&&(selectedOrders.length===0||selectedOrders.length>1);partsBox.innerHTML='';orderScope.forEach(function(oid){(orderParts[oid]||[]).forEach(function(pid){const key=partKey(oid,pid);const label=document.createElement('label');label.className='filter-check';const input=document.createElement('input');input.type='checkbox';input.name='filter-part';input.value=key;input.checked=prev.has(key);label.appendChild(input);label.appendChild(document.createTextNode(showOrderInLabel?partLabel(pid)+' ('+oid+')':partLabel(pid)));partsBox.appendChild(label);});});}function barVisible(bar,orderIds,partKeys){if(orderIds.length&&!orderIds.includes(bar.dataset.order))return false;if(partKeys.length&&!partKeys.includes(partKey(bar.dataset.order,bar.dataset.part)))return false;return true;}function colorForRank(rank,count){if(count<=1)return 'hsl(0,72%,42%)';const hue=Math.round((rank/(count-1))*120);return 'hsl('+hue+',72%,42%)';}function uniquePartIdsFromKeys(partKeys){return partKeys.map(function(k){return k.split('::')[1];}).filter(function(p,i,a){return p&&a.indexOf(p)===i;});}function maxRouteSequence(partId,visibleBars){const fromMeta=(scheduleMeta.partMaxSequence||{})[partId];if(fromMeta!=null)return Number(fromMeta);let max=0;visibleBars.forEach(function(bar){const s=Number(bar.dataset.sequence);if(!isNaN(s)&&s>=0)max=Math.max(max,s);});return max;}function resolveColorMode(orderIds){if(allOrderIds.length<=1)return 'part';if(orderIds.length===0)return 'order';if(orderIds.length>1)return 'order';return 'part';}function applyBarColors(visibleBars,mode,orderIds,partIds){if(!visibleBars.length)return;const uniqueParts=uniquePartIdsFromKeys(partIds);if(partIds.length>0&&uniqueParts.length===1){const partId=uniqueParts[0];const maxSeq=maxRouteSequence(partId,visibleBars);const stepCount=maxSeq+1;visibleBars.forEach(function(bar){if(bar.classList.contains('setup-bar')){bar.style.background='#ef6c00';return;}const seq=Number(bar.dataset.sequence);if(isNaN(seq)||seq<0){bar.style.background='#1565c0';return;}bar.style.background=colorForRank(seq,stepCount);});legendEl.textContent='Цвет по операциям маршрута («'+partLabel(partId)+'»): красный — первая, зелёный — последняя.';return;}if(mode==='order'){const scope=orderIds.length?orderIds.slice():allOrderIds.slice();scope.sort(function(a,b){return orderCreatedMs(a)-orderCreatedMs(b);});const rankById=new Map(scope.map(function(id,i){return [id,i];}));visibleBars.forEach(function(bar){if(bar.classList.contains('setup-bar')){bar.style.background='#ef6c00';return;}const rank=rankById.get(bar.dataset.order)||0;bar.style.background=colorForRank(rank,scope.length);});legendEl.textContent='Цвет по заказам: красный — раньше принят (выше в очереди), зелёный — позже.';}else{const partSet=new Set(visibleBars.map(function(b){return b.dataset.part;}));let scope=Array.from(partSet);if(partIds.length){scope=partIds.map(function(k){return k.split('::')[1];}).filter(function(p,i,a){return p&&partSet.has(p)&&a.indexOf(p)===i;});}scope.sort(function(a,b){return partPriority(b)-partPriority(a);});const rankById=new Map(scope.map(function(id,i){return [id,i];}));visibleBars.forEach(function(bar){if(bar.classList.contains('setup-bar')){bar.style.background='#ef6c00';return;}const rank=rankById.get(bar.dataset.part)||0;bar.style.background=colorForRank(rank,scope.length);});legendEl.textContent='Цвет по деталям: красный — выше приоритет детали в заказе, зелёный — ниже.';}}function applyFilters(){const orderIds=selectedValues(ordersBox);const partIds=selectedValues(partsBox);const mode=resolveColorMode(orderIds);let anyVisible=false;const visibleBars=[];panel.querySelectorAll('.machine-row').forEach(function(row){let rowHas=false;row.querySelectorAll('.machine-bar').forEach(function(bar){const show=barVisible(bar,orderIds,partIds)&&bar.dataset.outOfView!=='1';bar.style.display=show?'':'none';if(show){rowHas=true;if(bar.classList.contains('setup-bar')){bar.style.background='#ef6c00';}else{visibleBars.push(bar);}}});const filtering=orderIds.length||partIds.length;row.classList.toggle('filtered-out',filtering&&!rowHas);if(rowHas)anyVisible=true;});applyBarColors(visibleBars,mode,orderIds,partIds);updateMachinePartSpans(orderIds,partIds);const filtering=orderIds.length||partIds.length;emptyMsg.classList.toggle('hidden',!filtering||anyVisible);}function updateMachinePartSpans(orderIds,partIds){const filterParts=partIds.length>0;const filterOrders=orderIds.length>0;panel.querySelectorAll('.machine-row').forEach(function(row){const allEl=row.querySelector('.machine-span-all');let visiblePartCount=0;row.querySelectorAll('.machine-span-part').forEach(function(li){const key=partKey(li.dataset.order,li.dataset.part);const matchOrder=!filterOrders||orderIds.includes(li.dataset.order);const matchPart=!filterParts||partKeys.includes(key);let hasBar=false;row.querySelectorAll('.machine-bar').forEach(function(bar){if(bar.style.display==='none')return;if(bar.dataset.order===li.dataset.order&&bar.dataset.part===li.dataset.part){hasBar=true;}});const show=matchOrder&&matchPart&&hasBar;li.classList.toggle('hidden',!show);if(show)visiblePartCount++;});if(allEl){allEl.classList.toggle('hidden',filterParts&&visiblePartCount>0);}});}ordersBox.addEventListener('change',function(){rebuildPartFilters();applyFilters();});partsBox.addEventListener('change',applyFilters);resetBtn.addEventListener('click',function(){ordersBox.querySelectorAll('input').forEach(function(i){i.checked=false;});rebuildPartFilters();partsBox.querySelectorAll('input').forEach(function(i){i.checked=false;});applyFilters();});function panelViewport(){const panel=document.getElementById('machine-schedule-panel');return {start:Number(panel.dataset.viewStartMs||panel.dataset.fullStartMs),end:Number(panel.dataset.viewEndMs||panel.dataset.fullEndMs)};}function formatRangeLabel(startMs,endMs){const f=function(ms){return new Date(ms).toLocaleString('ru-RU',{timeZone:'Europe/Moscow',day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'});};return f(startMs)+' — '+f(endMs);}function axisStepMs(span){if(span<=6*3600000)return 3600000;if(span<=2*86400000)return 3*3600000;if(span<=10*86400000)return 86400000;return 2*86400000;}function formatTickLabel(ms,span,align){const d=new Date(ms);if(align==='mid'&&span>2*86400000){return d.toLocaleDateString('ru-RU',{timeZone:'Europe/Moscow',day:'2-digit',month:'2-digit'});}return span<=86400000? d.toLocaleTimeString('ru-RU',{timeZone:'Europe/Moscow',hour:'2-digit',minute:'2-digit'}): d.toLocaleString('ru-RU',{timeZone:'Europe/Moscow',day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'});}function rebuildTimeAxis(tl){const block=tl.closest('.timeline-block');if(!block)return;const axis=block.querySelector('.time-axis');if(!axis)return;const startMs=Number(tl.dataset.startMs);const endMs=Number(tl.dataset.endMs);const span=endMs-startMs;if(!span)return;const step=axisStepMs(span);axis.innerHTML='';function addTick(ms,align){const tick=document.createElement('span');tick.className='tick tick-'+align;if(align==='end'){tick.style.right='0';}else{const left=Math.min(100,Math.max(0,((ms-startMs)*100)/span));tick.style.left=left.toFixed(2)+'%';}tick.textContent=formatTickLabel(ms,span,align);axis.appendChild(tick);}addTick(startMs,'start');let lastPct=0;for(let t=startMs+step;t<endMs&&axis.childElementCount<12;t+=step){const pct=((t-startMs)*100)/span;if(pct-lastPct>=12&&pct<=88){addTick(t,'mid');lastPct=pct;}}addTick(endMs,'end');}function layoutTimeline(tl){const startMs=Number(tl.dataset.startMs);const endMs=Number(tl.dataset.endMs);const span=endMs-startMs;if(!span)return;const nowMs=Number(tl.dataset.nowMs);let nowEl=tl.querySelector('.now');if(nowMs>=startMs&&nowMs<=endMs){if(!nowEl){nowEl=document.createElement('div');nowEl.className='now';tl.appendChild(nowEl);}nowEl.style.left=Math.min(100,Math.max(0,((nowMs-startMs)*100)/span)).toFixed(2)+'%';nowEl.style.display='';}else if(nowEl){nowEl.style.display='none';}tl.querySelectorAll('.bar').forEach(function(bar){const bs=Number(bar.dataset.startMs);const be=Number(bar.dataset.endMs);const outOfView=be<=startMs||bs>=endMs;bar.dataset.outOfView=outOfView?'1':'0';if(outOfView){bar.style.display='none';return;}bar.style.display='';const visStart=Math.max(bs,startMs);const visEnd=Math.min(be,endMs);const left=((visStart-startMs)*100)/span;const width=Math.max(0.15,((visEnd-visStart)*100)/span);const clampedLeft=Math.min(99.85,left);const clampedWidth=Math.min(99.85-clampedLeft,width);bar.style.left=clampedLeft.toFixed(2)+'%';bar.style.width=clampedWidth.toFixed(2)+'%';});}function msToLocalInput(ms){return new Date(ms).toLocaleString('sv-SE',{timeZone:TZ}).replace(' ','T').slice(0,16);}function localInputToMs(s){if(!s)return NaN;return new Date(s+':00+03:00').getTime();}function syncCustomRangeInputs(){const vp=panelViewport();if(zoomCustomStart)zoomCustomStart.value=msToLocalInput(vp.start);if(zoomCustomEnd)zoomCustomEnd.value=msToLocalInput(vp.end);}function clearPresetButtonsActive(){document.querySelectorAll('.btn-zoom[data-zoom]').forEach(function(b){b.classList.remove('active');});}function updateNavButtons(){const preset=panel.dataset.zoomPreset||'';const disabled=preset==='row'||preset==='all';if(zoomPrevBtn)zoomPrevBtn.disabled=disabled;if(zoomNextBtn)zoomNextBtn.disabled=disabled;}function viewportSpanMs(){const vp=panelViewport();return Math.max(60000,vp.end-vp.start);}function setSharedViewport(startMs,endMs,options){options=options||{};if(!panel)return;const fullStart=Number(panel.dataset.fullStartMs);const fullEnd=Number(panel.dataset.fullEndMs);let start=Math.max(fullStart,startMs);let end=Math.min(fullEnd,endMs);if(end<=start)end=Math.min(start+3600000,fullEnd);panel.dataset.viewStartMs=start;panel.dataset.viewEndMs=end;panel.querySelectorAll('.machine-timeline').forEach(function(tl){tl.dataset.startMs=start;tl.dataset.endMs=end;layoutTimeline(tl);rebuildTimeAxis(tl);});if(zoomLabelEl)zoomLabelEl.textContent=formatRangeLabel(start,end);if(!options.skipCustomSync)syncCustomRangeInputs();updateNavButtons();}function panViewport(direction){const preset=panel.dataset.zoomPreset||'';if(preset==='row'||preset==='all')return;const fullStart=Number(panel.dataset.fullStartMs);const fullEnd=Number(panel.dataset.fullEndMs);const vp=panelViewport();const span=viewportSpanMs();let start=vp.start+direction*span;let end=start+span;if(start<fullStart){start=fullStart;end=Math.min(fullStart+span,fullEnd);}if(end>fullEnd){end=fullEnd;start=Math.max(fullEnd-span,fullStart);}clearPresetButtonsActive();panel.dataset.zoomPreset='pan';setSharedViewport(start,end);
applyFilters();}function applyCustomRange(){const startMs=localInputToMs(zoomCustomStart&&zoomCustomStart.value);const endMs=localInputToMs(zoomCustomEnd&&zoomCustomEnd.value);if(!Number.isFinite(startMs)||!Number.isFinite(endMs)||endMs<=startMs){alert('Укажите корректный интервал: конец позже начала.');return;}clearPresetButtonsActive();if(zoomCustomApply)zoomCustomApply.classList.add('active');panel.dataset.zoomPreset='custom';setSharedViewport(startMs,endMs,{skipCustomSync:true});
applyFilters();}function applyZoom(preset){if(!panel)return;panel.dataset.zoomPreset=preset;document.querySelectorAll('.btn-zoom[data-zoom]').forEach(function(b){b.classList.toggle('active',b.dataset.zoom===preset);});if(zoomCustomApply)zoomCustomApply.classList.remove('active');if(preset==='row'){panel.querySelectorAll('.machine-timeline').forEach(function(tl){const rs=Number(tl.dataset.rowStartMs);const re=Number(tl.dataset.rowEndMs);if(!rs||!re||rs>=re)return;tl.dataset.startMs=rs;tl.dataset.endMs=re;layoutTimeline(tl);rebuildTimeAxis(tl);});if(zoomLabelEl)zoomLabelEl.textContent='Свой интервал у каждого станка';
applyFilters();return;}if(preset==='all'){setSharedViewport(Number(panel.dataset.fullStartMs),Number(panel.dataset.fullEndMs));}else if(zoomPresets[preset]){const z=zoomPresets[preset];setSharedViewport(z[0],z[1]);}
applyFilters();updateNavButtons();}document.querySelectorAll('.btn-zoom[data-zoom]').forEach(function(btn){btn.addEventListener('click',function(){applyZoom(btn.dataset.zoom);});});if(zoomPrevBtn)zoomPrevBtn.addEventListener('click',function(){panViewport(-1);});if(zoomNextBtn)zoomNextBtn.addEventListener('click',function(){panViewport(1);});if(zoomCustomApply)zoomCustomApply.addEventListener('click',applyCustomRange);function refreshOrderTimelines(){document.querySelectorAll('.timeline:not(.machine-timeline)').forEach(function(tl){layoutTimeline(tl);rebuildTimeAxis(tl);});
}rebuildPartFilters();applyFilters();const initialPreset=panel.dataset.zoomPreset||'day';applyZoom(initialPreset);refreshOrderTimelines();})();
